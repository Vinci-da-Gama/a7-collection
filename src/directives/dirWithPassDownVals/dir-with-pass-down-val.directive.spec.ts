import { DirWithPassDownValDirective } from './dir-with-pass-down-val.directive';

describe('DirWithPassDownValDirective', () => {
	it('should create an instance', () => {
		const directive = new DirWithPassDownValDirective();
		expect(directive).toBeTruthy();
	});
});
