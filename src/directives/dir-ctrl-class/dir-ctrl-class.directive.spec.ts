import { DirCtrlClassDirective } from './dir-ctrl-class.directive';

describe('DirCtrlClassDirective', () => {
	it('should create an instance', () => {
		const directive = new DirCtrlClassDirective();
		expect(directive).toBeTruthy();
	});
});
