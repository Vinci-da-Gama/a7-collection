import { TeplViewcontentRefDirective } from './tmpl-viewcontent-ref.directive';

describe('TeplViewcontentRefDirective', () => {
	it('should create an instance', () => {
		const directive = new TeplViewcontentRefDirective();
		expect(directive).toBeTruthy();
	});
});
