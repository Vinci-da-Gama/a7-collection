// import { enableProdMode } from '@angular/core';

// when it run, it is already in server side, so just run it, no need environment config, the env is real prod anyway.
// enableProdMode();

export { AppServerModule } from './app/musaxi.server.module';
